#ddev-generated
# Mock Redis Commander Docker Compose configuration
services:
  redis-commander:
    image: rediscommander/redis-commander
    container_name: ddev-${DDEV_SITENAME}-redis-commander
    expose:
      - "8081"
    environment:
      - REDIS_HOST=redis