name: post-install-runtime-deps-addon
description: Test addon that creates runtime dependencies during post-install actions

pre_install_actions:
  - |
    #ddev-description:Pre-install setup
    echo "Pre-install actions for post-install runtime deps addon"

project_files:
  - config.post-install-runtime-deps-addon.yaml

post_install_actions:
  - |
    #ddev-description:Create runtime dependencies file during post-install
    echo "# Runtime dependencies discovered during post-install" > .runtime-deps-post-install-runtime-deps-addon
    echo "mock/mock-redis" >> .runtime-deps-post-install-runtime-deps-addon
    echo "" >> .runtime-deps-post-install-runtime-deps-addon
    echo "# This is a comment" >> .runtime-deps-post-install-runtime-deps-addon
    echo "mock/mock-redis-commander" >> .runtime-deps-post-install-runtime-deps-addon
    echo "Post-install runtime dependencies file created"
