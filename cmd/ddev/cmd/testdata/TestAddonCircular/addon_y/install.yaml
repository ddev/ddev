name: addon-y
dependencies:
  - ../addon_z
description: Test addon Y that depends on addon Z (part of circular dependency chain)

project_files:
  - y-config.yaml

post_install_actions:
  - |
    #ddev-description:Addon Y installation complete
    echo "Addon Y installed successfully"