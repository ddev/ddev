name: addon-z
dependencies:
  - ../addon_x
description: Test addon Z that depends on addon X (completes circular dependency chain)

project_files:
  - z-config.yaml

post_install_actions:
  - |
    #ddev-description:Addon Z installation complete
    echo "Addon Z installed successfully"