# this variable overrides the value from .env.busybox
SERVICE_OVERRIDE_VARIABLE=service_local_value
# a secret for the busybox add-on, read by its install actions, kept out of Git
SECRET_VARIABLE=secret_value
