# this variable will not be passed to the containers because we don't have redis service,
# but it will be passed to the 'docker-compose config' build step,
# in result we can expand this variable in any docker-compose file, such as docker-compose.busybox.yaml
REDIS_TAG=7
