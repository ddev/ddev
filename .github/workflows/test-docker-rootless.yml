name: Test Docker Rootless
on:
  pull_request:
    paths:
      - "go.*"
      - "pkg/**"
      - "cmd/**"
      - "Makefile"
      - "vendor/**"
      - ".github/workflows/**"
      - "!.github/workflows/docs**"
  push:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      debug_enabled:
        description: 'Run the build with tmate set "debug_enabled"'
        type: boolean
        required: false
        default: false
      ddev_embargo_tests:
        description: 'Comma-separated list of test names to skip'
        type: string
        required: false
        default: ''

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  docker-rootless:
    uses: ./.github/workflows/test-reusable.yml
    secrets: inherit
    with:
      ddev_test_docker_rootless: "true"
      ddev_test_no_bind_mounts: "true"
      ddev_embargo_tests: ${{ inputs.ddev_embargo_tests || '' }}
      debug_enabled: ${{ inputs.debug_enabled || false }}
