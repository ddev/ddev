name: Windows container tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    name: Run tests
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2

    - name: Run tests
      run: ./.github/workflows/test_containers.cmd
