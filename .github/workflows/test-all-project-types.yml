name: Test All Project Types
on:
  pull_request:
    paths:
      - "go.*"
      - "pkg/**"
      - "cmd/**"
      - "Makefile"
      - "vendor/**"
      - ".github/workflows/**"
      - "!.github/workflows/docs**"
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      debug_enabled:
        description: 'Run the build with upterm set "debug_enabled"'
        type: boolean
        required: false
        default: false
      testargs:
        description: 'TESTARGS value'
        type: string
        required: false
        default: ''
      gotest_short:
        description: 'GOTEST_SHORT value (16=drupal11 only)'
        type: string
        required: false
        default: '16'
      make_target:
        description: 'Make target to run (custom job only)'
        type: choice
        required: false
        default: 'test'
        options:
          - test
          - testpkg
          - testcmd
          - testddevapp
          - testnotddevapp
          - testfullsitesetup
          - testonepkg
          - testonefile
      makeargs:
        description: 'Extra make arguments: TESTPKG=./pkg/util (for testonepkg) or TESTFILE=./pkg/util/utils_test.go (for testonefile)'
        type: string
        required: false
        default: ''

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  determine-test-eligibility:
    name: Determine test eligibility
    uses: ./.github/workflows/test-skip-check.yml

  prepare-testargs:
    needs: determine-test-eligibility
    if: github.event_name != 'workflow_dispatch' && needs.determine-test-eligibility.outputs.should_skip != 'true'
    runs-on: ubuntu-latest
    outputs:
      testargs: ${{ steps.compute-testargs.outputs.testargs }}
    steps:
      - name: Compute TESTARGS
        id: compute-testargs
        run: |
          # Run all tests that limit their scope when running with any value of GOTEST_SHORT
          TP=(
            "TestAppdirAlreadyInUse"
            "TestAutocomplet"
            "TestAvailableUpdates"
            "TestComposerCreateCmd"
            "TestCmdDescribe"
            "TestCmdList"
            "TestCmdStart"
            "TestCmdStop"
            "TestConfigDatabaseVersion"
            "TestConfigValidate"
            "TestDdevAllDatabases"
            "TestDdevFullSite.*"
            "TestDdevImportFiles.*"
            "TestDdevUploadDir.*"
            "TestDdevXdebugEnabled"
            "TestDdevXhprofPrependEnabled"
            "TestDdevXhprofXhguiEnabled"
            "TestGetApps.*"
            "TestPHPConfig"
            "TestPHPWebserverType"
            "TestWriteDrushConfig"
          )
          TESTARGS="-run '($(IFS="|"; echo "${TP[*]}"))'"
          echo "testargs=${TESTARGS}"
          echo "testargs=${TESTARGS}" >> $GITHUB_OUTPUT

  all-project-types:
    if: github.event_name != 'workflow_dispatch' && needs.determine-test-eligibility.outputs.should_skip != 'true'
    needs: [determine-test-eligibility, prepare-testargs]
    uses: ./.github/workflows/test-reusable.yml
    secrets: inherit
    with:
      gotest_short: "" # run tests for all project types
      testargs: ${{ needs.prepare-testargs.outputs.testargs }}
      debug_enabled: ${{ inputs.debug_enabled || false }}

  all-project-types-custom:
    needs: determine-test-eligibility
    if: github.event_name == 'workflow_dispatch' && needs.determine-test-eligibility.outputs.should_skip != 'true'
    uses: ./.github/workflows/test-reusable.yml
    secrets: inherit
    with:
      make_target: ${{ inputs.make_target }}
      makeargs: ${{ inputs.makeargs }}
      testargs: ${{ inputs.testargs }}
      gotest_short: ${{ inputs.gotest_short }}
      debug_enabled: ${{ inputs.debug_enabled || false }}
