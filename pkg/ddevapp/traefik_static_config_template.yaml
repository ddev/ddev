#ddev-generated

log:
  level: DEBUG
api:
  insecure=true
providers:
  file:
    directory: /mnt/ddev-global-cache/traefik/config
    watch: true
global:
  sendanonymoususage: false

entryPoints:
  {{ range $port := .RouterPorts}}
  http-{{$port}}:
    address: ":{{$port}}"
  {{ end }}
