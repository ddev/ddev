#ddev-generated

log:
  level: DEBUG
accesslog: { }

api:
  dashboard: true
  insecure: true

providers:
  file:
    directory: /mnt/ddev-global-cache/traefik/config
    watch: true
global:
  sendanonymoususage: false

entryPoints:
  traefik:
    address: ":9999"
  {{ range $port := .RouterPorts}}
  http-{{$port}}:
    address: ":{{$port}}"
  {{ end }}
