
services:
  nginx:
    image: nginx
    container_name: ddev-${DDEV_SITENAME}-nginx
    ports:
      - 80
    environment:
      - VIRTUAL_HOST=extra.ddev.site
      - HTTP_EXPOSE=80:80
      - HTTPS_EXPOSE=443:80
