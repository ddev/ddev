#ddev-generated

http:
  routers:
    {{ $appname := .App.Name}}{{ range $s := .RoutingTable }}
    {{- if not $s.HTTPS -}}
    {{ $appname }}-{{ $s.InternalServiceName }}-{{ $s.InternalServicePort }}-http:
      entrypoints:
        - http-{{$s.ExternalPort}}
      {{- if not $.UseLetsEncrypt -}}{{/* Let's Encrypt only works with Host(), but we need HostRegexp() for wildcards*/}}
      rule: HostRegexp({{ range $i, $h := $s.ExternalHostnames }}{{if $i}}, {{end}}`{{$h}}`{{end}})
      {{ else }}
      rule: Host({{ range $i, $h := $s.ExternalHostnames }}{{if $i}}, {{end}}`{{$h}}`{{end}})
      {{ end }}
      service: "{{$appname}}-{{$s.InternalServiceName}}-{{$s.InternalServicePort}}-http"
      tls: false
      # middlewares:
      #   - "redirectHttps"
    {{ end }}{{ end }}
    {{ range $s := .RoutingTable }}
      {{- if $s.HTTPS -}}
    {{$appname}}-{{$s.InternalServiceName}}-{{$s.InternalServicePort}}-https:
      entrypoints:
        - http-{{$s.ExternalPort}}
      {{- if not $.UseLetsEncrypt -}}{{/* Let's Encrypt only works with Host(), but we need HostRegexp() for wildcards*/}}
      rule: HostRegexp({{ range $i, $h := $s.ExternalHostnames }}{{ if $i }}, {{ end }}`{{$h}}`{{ end }})
      {{ else }}
      rule: Host({{ range $i, $h := $s.ExternalHostnames }}{{- if $i -}}, {{- end -}}`{{$h}}`{{end}})
      {{ end }}
      service: "{{$appname}}-{{$s.InternalServiceName}}-{{$s.InternalServicePort}}-http"
      {{ if not $.UseLetsEncrypt }}
      tls: true
      {{ else }}
      tls:
        certResolver: acmeresolver
      {{ end }}{{ end }}
    {{ end }}

  middlewares:
    redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    {{$appname := .App.Name}}
    {{- $outputServices := dict -}}
    {{ range $s := .RoutingTable }}
      {{- if not (index $outputServices (print $s.ServiceName)) -}}
        {{- $outputServices = merge $outputServices (dict (print $s.ServiceName) true) -}}
    {{$appname}}-{{ $s.ServiceName }}:
      loadbalancer:
        servers:
          - url: http://ddev-{{$appname}}-{{$s.Service.InternalServiceName}}:{{$s.Service.InternalServicePort}}
        {{ end }}
      {{ end }}

{{/* let's encrypt doesn't work if there's already a provided cert, so omit there */}}
{{- if not .UseLetsEncrypt -}}
tls:
  certificates:
    - certFile: {{ .TargetCertsPath }}/{{ .App.Name }}.crt
      keyFile: {{ .TargetCertsPath }}/{{ .App.Name }}.key
{{- end -}}
