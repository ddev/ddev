[mysqld]

# caching_sha2_password is the default for mysql 8.0 and 8.4, no config required
# default-authentication-plugin=caching_sha2_password

# enable mysql-native-password only in case it needs to be used by php 7.3 and below
# This is removed in MySQL 9
mysql-native-password=1

# In mysql 8+ this replaces innodb-log-file-size etc
innodb-redo-log-capacity=100663296
